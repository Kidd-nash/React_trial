class Crud {
    //CRUD for reminders
    //Create
    create (task) {
        console.log("creating");
        console.log(task);
    }
    //Retrieve
    retrieve () {
        console.log("retrieving");
    }
    //Update
    update (task) {
        console.log("updating");
        console.log(task);
    }
    //Delete
    delete (task) {
        console.log("deleting");
        console.log(task);
    }
}